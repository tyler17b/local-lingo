[project]
name = "local-lingo"
version = "0.1.0a3"
description = "Local-first language learning for Home Assistant"
requires-python = ">=3.13"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"

[tool.ruff]
target-version = "py313"
line-length = 88
